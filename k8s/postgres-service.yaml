apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: educard-prod
  labels:
    app: postgres
    component: database
spec:
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  clusterIP: None  # Headless service for StatefulSet
  type: ClusterIP
